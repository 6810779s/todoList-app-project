import React from 'react';
import styles from '../styles/Container.module.css';

const Container = () => {
  return (
    <div className={styles.container}>
      <top className={styles.title}>
        <h1>Planner</h1>
      </top>
    </div>);
};

export default Container;
